---
title: 2026-07-13
type: daily
created: 2026-07-13
tags: [course, week-3, cli]
---

# Monday, July 13, 2026 — Week 3, Day 9

## 📥 Captured today
- **Day 9: built "the doorway"** — a `vault` CLI (`~/vault/bin/vault`) that checks every write **at the entry**. Four verbs: capture · adr · task · done (+ help; refuses unknown verbs). Refuse → reason on stderr + exit 2; accept → receipt + exit 0.
- Proved it with the "three bad parcels": empty words · unknown type · duplicate → three refusals, exit 2 each.
- **Rewired the `my-vault` skill** so the AI runs `vault capture`/`vault adr` instead of hand-writing files — human and AI now use the same door.
- Personalized a refusal message + added a help example (make-it-yours step).
- The doctor still checks *after*; the doorway checks *before* → [[the-doorway-cli]].

## 🗂 Filed into
- [[the-doorway-cli]] — Day 9 concept (before vs after)

- Extended the doorway with a **plan** verb (a scope built on *multiple* ADRs) and made **task** require `--plan` — grew CLAUDE.md + the doctor with a new `plan` type (they move together). Then used the pipeline to **set the v4 scopes** from the ADR backlog: `plan-v4-account-safety` (4 tasks) + `plan-v4-sync-across-devices` (2 tasks). Every task traces task → plan → ADRs. → [[the-doorway-cli]]

## 💭 Open threads
- "The wall still has a hole" — you can still bypass the door by writing files directly. Day 10 bricks it up.
