---
title: 2026-07-07
type: daily
created: 2026-07-07
tags: [course, week-2, hooks]
---

# Tuesday, July 7, 2026 — Week 2, Day 6

## 📥 Captured today
- **Day 6: built the guard hook.** Turned yesterday's *promise* (the STOP line in the `my-vault` skill) into a *law*: a `PreToolUse` hook (`~/.claude/hooks/vault-write-guard.sh`) that runs before every Bash action.
- Wrote **two laws**: block `git commit`/`git push` (*"committing is the human's signature"*) and block `rm …vault` (*"deleting in the vault needs your hands"*), each with `exit 2`.
- Registered it in `~/.claude/settings.json` under `PreToolUse` / matcher `Bash`.
- **Proved it live** — a real AI `git commit` attempt was *blocked* with the human-authored reason; a plain read ran free (`exit 0`); a `rm …vault` was blocked too.
- Learned the core distinction — promise vs. law → [[the-guard-hook]].

## 🗂 Filed into
- [[the-guard-hook]] — new knowledge note (Day 6 concept)

## 💭 Open threads
- Day 7 question: the guard checks what the AI *does*, never what a note *says*. Who catches a wrong type or missing link?
