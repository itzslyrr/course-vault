---
title: 2026-07-16
type: daily
created: 2026-07-16
tags: [course, week-3, cli, retrieval]
---

# Thursday, July 16, 2026 — Week 3, Day 12

## 📥 Captured today
- **Day 12: Ship Something Real.** The week's finish — point the whole system at one real build. The framing note is **the three-channel loop**: ① *command* (you → AI, you chat/research/decide) → ② *action* (the AI acts **only** through the one governed doorway) → ③ *perception* (you run it and gate it on your surface, which sets up the next command). Autonomy grows by widening ②; the system stays human by strengthening ③. → [[the-three-channel-loop]]
- The vocabulary named in one place: **the surface** (any dual-legible artifact you and the AI both read/write — the load-bearing idea), **the CLI/doorway** (channel ②), **the Hook** (deterministic fail-closed enforcement — guards the path), **the doctor** (write-format check — required fields · known type · real zone), **the Human Gate** (the AI proposes, you ratify — the author never signs off its own work). **Trustworthy autonomy = judgment (Gate) + enforcement (Hook) + one doorway (CLI).** And *vibe-coding* (a real thing built on a governed trail you can audit) vs *vibe-slop* (output nobody can check).
- The method for driving any build: research → record → project → ADR → plan → scope → tasks → build → run → showcase. The vault holds the trail; the AI does the work; you hold the judgment.
- **Today's actual build — gave the CLI its read side.** Until now `bin/vault` was write-only; its help even said *"reads arrive on Day 11."* Implemented the Day-12 cheat-sheet's read verbs — `search`, `recent`, `tree`, plus `open`, `backlinks`, `related`, `tags`. Reads run **free** (no gate, nothing written) — they *are* channel ③ perception. This closes the loop the door was only half of. → [[the-door-reads-both-ways]]
- The handout also introduces a new write verb, **`scope`**, sitting between `plan` and `task` (`plan` now just names an outcome; `scope` names a slice; `task ... --scope <slug>`). Our current CLI still uses the older `plan <adr-refs>` + `task --plan` model — noted as an open thread, not changed today (would touch the existing task-manager trail).

## 🗂 Filed into
- [[the-three-channel-loop]] — Day 12 concept (command · action · perception)
- [[the-door-reads-both-ways]] — Day 12 build (the CLI's read verbs)

## 💭 Open threads
- **The Pulse** — the Day 12 build assignment: topic in → clean list out, one platform, sad path handled, driven end-to-end through the vault (project · ADR · plan · scope · 3–4 tasks). Not started; my own topic to pick.
- Align the CLI's write model to Day 12's `plan → scope → task` (add a `scope` verb, relax `plan`), without breaking task-manager's existing `--plan` work items.
- Backfill empty `tags: []` on `~/vault` captures so `vault search`/`vault tags` have a full dimension to work with.
