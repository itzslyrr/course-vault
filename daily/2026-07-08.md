---
title: 2026-07-08
type: daily
created: 2026-07-08
tags: [course, week-2, housekeeping]
---

# Wednesday, July 8, 2026 — Week 2

## 📥 Captured today
- Built a **Day-5 visual explainer** (`course/day5-explainer.html`) — skills / ADRs / hooks, promise-vs-wall, linked on the dashboard.
- Added a **finish-sound** notification hook (a bubble `Pop`) that plays when a turn completes.
- **Consolidated the vault.** Work had split across two folders (`BASH1` vs a stray `BASH 1` with a space) plus an other-account session. Merged everything into the real `BASH1/Vault`: the other-account's [[the-house-rule]] note, the `decisions/` ADR, and `review.html`, together with this session's day5-explainer and dashboard page-links. Backfilled the missing Day-6 records.
- Disabled the vault-write-guard (it had served its Day-6 teaching purpose).
- **Day 7: built "the doctor"** (`~/vault/doctor.sh`) — a read-only rule-checker. Found & fixed a bug (it walked `projects/*.md` instead of `projects/*/*.md`, silently skipping every project note). Proved it: broke a note on purpose (`type: leraning`, no `created`, dead link) → doctor caught all 3 with file+problem → fixed → clean bill of health. Learned the routine: **draft → doctor → diff → commit** → [[the-doctor]].

- **End-of-week project: Task Manager v1.** Built a working single-screen task app (create/list/complete/delete, newest-first, localStorage) + a **set of 5 ADRs** documenting every decision, in `~/vault/projects/task-manager/`. Passed the doctor (clean). → [[task-manager]]
- **Task Manager v2: Kanban.** Researched Kanban (visualize workflow, limit WIP), then applied it: a 3-column board (To Do / In Progress / Done), drag-and-drop + button moves, **WIP limit of 3** on In Progress, `column` data model migrating v1. Documented as **ADR-006…010**; sync re-prioritized to v3. Doctor clean. → [[task-manager]]
- **Task Manager v2 on SvelteKit + SQLite.** Instructor wanted v2 on a real stack. Installed Node 22, built a server-backed SvelteKit app with a **better-sqlite3** database, a REST API, and the **WIP limit enforced server-side (HTTP 409)**. Runs at localhost:5180. Documented as **ADR-011…013**. The doctor caught a dangling link — fixed it. → [[task-manager]]
- **Task Manager v3: multi-user auth.** Instructor's third brief. Built a real login system (register/login/logout) on the SvelteKit+SQLite app: **scrypt-hashed passwords**, **httpOnly session cookies** checked in `hooks.server.js`, **per-user data** (`user_id` on every task, per-user WIP). Verified in a real browser: data isolation, persistence, wrong-password rejection. Runs at localhost:5181. Documented as **ADR-014…017**. → [[task-manager]]
- Built the **Task Manager presentation deck** (`course/task-manager-deck.html`) with **real screenshots** of the running app (default board + WIP-full + v3 login/board). Caught that a stray *TravelManager* project had grabbed port 5173.

## 🗂 Filed into
- [[task-manager]] — new project note (Week 2 end-of-week deliverable)
- [[the-doctor]] — Day 7 concept (rules → checks)
- [[the-guard-hook]], [[the-house-rule]] — Day 5/6 concepts now recorded
- daily/2026-07-07 — Day 6 lab log

## 💭 Open threads
- Week 4 (Jul 20–23): final presentation to the boss — the scheduled `course-final-summary` task compiles it, then I turn it into a polished deck.
